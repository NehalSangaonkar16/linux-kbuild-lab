# linux-kbuild-lab
Hands-on Linux kernel project exploring Kconfig and Kbuild internals, including conditional compilation, multi-object modules, built-in vs modular drivers, and incremental builds using the kernel build system.
